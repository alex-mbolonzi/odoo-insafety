<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="1">
        <record id="ir_cron_consolidate_last_month" model="ir.cron">
            <field name="name">Monthly Rent Invoices</field>
            <field name="model_id" ref="model_insafety_property_rent_contract"/>
            <field name="state">code</field>
            <field name="code">model._create_invoices()</field>
            <field name="active" eval="True"/>
            <field name="interval_number">1</field>
            <field name="interval_type">months</field>
            <field name="nextcall" eval="(DateTime.now() + relativedelta(day=1, months=1)).strftime('%Y-%m-%d 04:00:00')" />
        </record>
        <!-- <record id="ir_cron_consolidate_monthly_invoices" model="ir.cron">
            <field name="cron_name">Monthly Rent Invoices</field> <field name="ir_actions_server_id" ref="action_server_monthly_rent_invoices"/> <field name="active" eval="True"/>
            <field name="user_id" ref="base.user_root"/>
            <field name="interval_number">1</field>
            <field name="interval_type">months</field>
            <field name="nextcall" eval="(DateTime.now() + relativedelta(day=1, months=1, hour=4, minute=0, second=0)).strftime('%Y-%m-%d %H:%M:%S')"/>
            </record> -->

        <!-- <record id="action_server_generate_monthly_statements" model="ir.actions.server">
            <field name="name">Generate Monthly Statements Action</field>
            <field name="model_id" ref="model_insafety_property_building"/>
            <field name="state">code</field>
            <field name="code">model._cron_generate_monthly_statements()</field>
            <field name="binding_model_id" eval="False"/>
        </record> -->

        <!-- <record id="ir_cron_generate_monthly_statements" model="ir.cron">
            <field name="cron_name">Generate Monthly Statements</field>
            <field name="ir_actions_server_id" ref="action_server_generate_monthly_statements"/>
            <field name="active" eval="True"/>
            <field name="user_id" ref="base.user_root"/>
            <field name="interval_number">1</field>
            <field name="interval_type">months</field>
            <field name="nextcall" eval="(DateTime.now() + relativedelta(day=1, months=1, hour=1, minute=0, second=0)).strftime('%Y-%m-%d %H:%M:%S')"/>
        </record> -->
    </data>
</odoo>